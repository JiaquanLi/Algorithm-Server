add_library(dcl STATIC IMPORTED GLOBAL)

set_property(TARGET dcl 
	PROPERTY INTERFACE_INCLUDE_DIRECTORIES 
		${CMAKE_CURRENT_SOURCE_DIR}/include)

set_property(TARGET dcl 
	PROPERTY IMPORTED_LOCATION 
		${CMAKE_CURRENT_SOURCE_DIR}/lib/libdcl.a)
